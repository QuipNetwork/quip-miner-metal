/target
.cargo/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# rustcheck working artifacts (audit reports)
.rustcheck/

# Serena MCP server workspace metadata (per-machine, not project code)
.serena/

# Agent tooling config (per-machine / per-tool; AGENTS.md is the checked-in source)
.beads/
.agents/
.claude/
.codex/
CLAUDE.md

# Python bytecode from the probe scripts
__pycache__/
